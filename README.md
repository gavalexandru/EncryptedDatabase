# EncryptedDatabase

CLI application that encrypts files using asymmetric cryptography, securely manages public/private keys, and enables safe storage, retrieval, and deletion of files with guaranteed data integrity.
